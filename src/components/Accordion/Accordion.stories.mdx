import { useState, useMemo, useEffect } from 'react'
import { Source, ArgsTable } from '@storybook/components'
import { Meta, Story, Preview, Canvas, Subtitle } from '@storybook/addon-docs'
import { Container } from 'decentraland-ui/dist/components/Container/Container'
import { Stats } from 'decentraland-ui/dist/components/Stats/Stats'
import { Card } from 'decentraland-ui/dist/components/Card/Card'
import Accordion from './Accordion'
import Divider from '../Text/Divider'
import Paragraph from '../Text/Paragraph'
import ImgFixed from '../Image/ImgFixed'
import Land from '../../utils/api/Land.ts'

<Meta title="components/Accordion" />

## With content

<Canvas isExpanded>
  <Story name="With content">
    <Container>
      <Accordion
        title={<Stats title="Accordion sub title">Accordion title</Stats>}
        description={
          <Paragraph secondary small>
            Accordion description
          </Paragraph>
        }
      >
        <ImgFixed src={Land.get().getParcelImage([-29, 55])} dimension="wide" />
      </Accordion>
    </Container>
  </Story>
</Canvas>

<Divider size="small" />

## Without content

<Canvas isExpanded>
  <Story name="Without content">
    <Container>
      <Accordion
        title={<Stats title="Accordion sub title">Accordion title</Stats>}
        description={
          <Paragraph secondary small>
            Accordion description
          </Paragraph>
        }
      />
    </Container>
  </Story>
</Canvas>

<Divider size="small" />

## With content and Card

<Canvas isExpanded>
  <Story name="With content and Card">
    <Card style={{ width: '100%' }}>
      <Card.Content>
        <Accordion
          title={<Stats title="Accordion sub title">Accordion title</Stats>}
          description={
            <Paragraph secondary small>
              Accordion description
            </Paragraph>
          }
        >
          <ImgFixed
            src={Land.get().getParcelImage([-108, -30])}
            dimension="wide"
          />
        </Accordion>
      </Card.Content>
    </Card>
  </Story>
</Canvas>

<Divider size="small" />

## Without content and Card

<Canvas isExpanded>
  <Story name="Without content and Card">
    <Card style={{ width: '100%' }}>
      <Card.Content>
        <Accordion
          title={<Stats title="Accordion sub title">Accordion title</Stats>}
          description={
            <Paragraph secondary small>
              Accordion description
            </Paragraph>
          }
        />
      </Card.Content>
    </Card>
  </Story>
</Canvas>

<Divider size="small" />
