import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Divider from '../Text/Divider'
import IntlStorybookProvider from '../Storybook/IntlStorybookProvider'
import Attending from './Attending'
import { event } from '../../__mocks__/event'

<Meta title="components/Button/Attending" />

<Canvas>
  <Story name="Jump In">
    <IntlStorybookProvider>
      <Attending
        event={event}
        onUpdateAttendee={() => {}}
        onNotify={() => {}}
        href="https://events.decentraland.org"
      />
    </IntlStorybookProvider>
  </Story>
</Canvas>

<Divider />

<Canvas>
  <Story name="Want To Go">
    <IntlStorybookProvider>
      <Attending
        event={{
          ...event,
          next_start_at: new Date().setDate(new Date().getDate() + 1),
        }}
        onUpdateAttendee={() => {}}
        onNotify={() => {}}
        href="https://events.decentraland.org"
      />
    </IntlStorybookProvider>
  </Story>
</Canvas>

<Divider />

<Canvas>
  <Story name="Loading">
    <IntlStorybookProvider>
      <Attending
        event={event}
        onUpdateAttendee={() => {}}
        onNotify={() => {}}
        href="https://events.decentraland.org"
        loading={true}
      />
    </IntlStorybookProvider>
  </Story>
</Canvas>
