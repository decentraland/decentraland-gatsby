import { useState, useMemo, useEffect } from 'react'
import { Source, ArgsTable } from '@storybook/components';
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { Button } from 'decentraland-ui/dist/components/Button/Button';
import Avatar from './Avatar';
import { Args } from '../Storybook/utils';
import Container from '../Storybook/Container';
import Divider from '../Text/Divider';
import { Time } from '../Date/utils';

<Meta title="Components/Profile/Avatar" component={Avatar} />

# Avatar component

> The *clipboard copy hook* allows to copy an arbitrary string to the user clipboards.
